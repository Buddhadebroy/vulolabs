pnpm-lock.yaml

# Node
node_modules/

# Composer
vendor/
composer.lock

# Build outputs
/plugins/*/assets/
release/
dist/
.cache/
.wireit/
build/

# Logs
log/
*.log

# PHPUnit
.phpunit.result.cache

# Docker
docker-compose.yml

# wp-env local secret overrides — per @wordpress/env's own
# .wp-env.override.json mechanism, loaded into the container's own
# wp-config.php and deliberately never committed (see
# plugins/vulopilot/config.php's VULOPILOT_GOOGLE_CLIENT_ID/SECRET
# docblock for why this exists).
.wp-env.override.json

# CI
.circleci/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Temp
tmp/
temp/